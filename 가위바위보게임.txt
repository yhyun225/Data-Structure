#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int rsp(void);
int main()
{
	srand((int)time(NULL));

	int win=0, draw=0, lose=0,challenge=7;
	int rock = 1, sic = 2, pap = 3;
	int choice;

	printf("바위 : 1 // 가위 : 2 // 보 : 3\n");
	
	while (challenge !=0)
	{
		scanf("%d", &choice);
		if (choice == 1)
		{
			if (rsp() == 1)
			{
				draw++;
				printf("당신 : 바위 ,컴퓨터 : 바위->비겼습니다!\n");
				printf("win : %d // draw: %d // lose: %d // 남은 기회 : %d\n", win, draw, lose, challenge);
			}
			else if (rsp() == 2)
			{
				win++;
				printf("당신 : 바위 ,컴퓨터 : 가위->이겼습니다!\n");
				printf("win : %d // draw: %d // lose: %d // 남은 기회 : %d\n", win, draw, lose, challenge);
			}
			else
			{
				lose++;
				challenge--;
				printf("당신 : 바위 ,컴퓨터 : 보->졌습니다!\n");
				printf("win : %d // draw: %d // lose: %d // 남은 기회 : %d\n", win, draw, lose, challenge);
			}
		}
		if (choice == 2)
		{
			if (rsp() == 2)
			{
				draw++;
				printf("당신 : 가위 ,컴퓨터 : 가위->비겼습니다!\n");
				printf("win : %d // draw: %d // lose: %d // 남은 기회 : %d\n", win, draw, lose, challenge);
			}
			else if (rsp() == 3)
			{
				win++;
				printf("당신 : 가위 ,컴퓨터 : 보->이겼습니다!\n");
				printf("win : %d // draw: %d // lose: %d // 남은 기회 : %d\n", win, draw, lose, challenge);
			}
			else
			{
				lose++;
				challenge--;
				printf("당신 : 가위 ,컴퓨터 : 바위->졌습니다!\n");
				printf("win : %d // draw: %d // lose: %d // 남은 기회 : %d\n", win, draw, lose, challenge);
			}
		}
		else if (choice == 3)
		{
			if (rsp() == 3)
			{
				win++;
				printf("당신 : 보 ,컴퓨터 : 보->비겼습니다!\n");
				printf("win : %d // draw: %d // lose: %d // 남은 기회 : %d\n", win, draw, lose, challenge);
			}
			else if (rsp() == 1)
			{
				win++;
				printf("당신 : 보 ,컴퓨터 : 바위->이겼습니다!\n");
				printf("win : %d // draw: %d // lose: %d // 남은 기회 : %d\n", win, draw, lose, challenge);
			}
			else
			{
				lose++;
				challenge--;
				printf("당신 : 보 ,컴퓨터 : 가위->졌습니다!\n");
				printf("win : %d // draw: %d // lose: %d // 남은 기회 : %d\n", win, draw, lose, challenge);
			}
		}
	}
	printf("\n%d승 %d무 %d패", win, draw, lose);

	return 0;


	

}
int rsp(void)
{
	return (rand() % 3)+1;
}